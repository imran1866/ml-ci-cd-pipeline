- name: Deploy on Render
  run: curl -X POST "https://api.render.com/v1/services/<your-service-id>/deploys" \
       -H "Accept: application/json" \
       -H "Authorization: Bearer ${{ secrets.RENDER_API_KEY }}" \
       -d ''

